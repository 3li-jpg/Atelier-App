{
  "name": "atelier",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "test": "npm test --workspaces --if-present",
    "dev": "npm run dev -w apps/api",
    "dev:web": "npm run dev -w @atelier/web"
  }
}
